# Copy to ".env" (gitignored) at the repo root and fill in the real values.
# Used by docker-compose.prod.yml — the only two things that differ between
# a local checkout and the production deploy on astralex.dev.
DND_DB_PASSWORD=
DND_CORS_ORIGIN=https://dnd.astralex.dev
